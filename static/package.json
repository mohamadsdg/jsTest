{
  "name": "static",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "babel src --out-dir build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint src ",
    "lint:CustomPathIgnore": "eslint --ignore-path ../.gitignore src",
    "prettier": "prettier --ignore-path ../.gitignore \"**/*.(js|json)\" ",
    "format:when-using-npm": "npm run prettier -- --write",
    "format": "yarn run prettier --write",
    "check-format": "yarn run prettier --list-different ",
    "validate": "yarn run check-format && yarn run lint:CustomPathIgnore && yarn run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.9.6",
    "@babel/preset-env": "^7.9.6",
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.11.0",
    "prettier": "^2.0.5"
  }
}
